services:
  backend:
    environment:
      CORS_ALLOWED_ORIGINS: "http://localhost,http://localhost:5173,http://127.0.0.1:5173,https://tenantra.homsi-co.com,http://tenantra.homsi-co.com,https://tenantracloud.com,https://www.tenantracloud.com"
      CORS_ALLOWED_METHODS: "GET,POST,PUT,DELETE,OPTIONS"
      CORS_ALLOWED_HEADERS: "Authorization,Content-Type"
      CORS_ALLOW_CREDENTIALS: "true"
      CORS_MAX_AGE: "600"
