global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: 'tenantra-backend'
    static_configs:
      - targets: ['backend:5000']
    metrics_path: /metrics
